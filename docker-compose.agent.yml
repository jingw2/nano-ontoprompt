# E0-IMAGES: milestone Agent deployment (digest-pinned, guarded-launcher).
#
# Every Python service starts through the guarded launcher
# (`scripts/guarded_entrypoint.py`, which refuses unsupported Python before any
# service import) and first verifies the signed build manifest baked at image
# build time (/manifests/agent.json).  Migrations route through E0-DB's
# `run_migrations.py`.  Image refs match the roles pinned by
# `scripts/generate_build_manifest.py`.
name: ontoprompt-agent

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ontoprompt
      POSTGRES_USER: ontoprompt
      POSTGRES_PASSWORD: ontoprompt
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ontoprompt -d ontoprompt"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  migration:
    image: ontoprompt-agent-api:agent-v1
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python scripts/run_migrations.py upgrade head"

  api:
    image: ontoprompt-agent-api:agent-v1
    ports: ["8000:8000"]
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
      REDIS_URL: redis://redis:6379/0
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"

  dispatcher:
    image: ontoprompt-agent-dispatcher:agent-v1
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
      REDIS_URL: redis://redis:6379/0
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python -m celery -A app.tasks.celery_app worker --loglevel=warning --concurrency=1 -n dispatcher@%h"

  artifact_worker:
    image: ontoprompt-agent-worker:agent-v1
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
      REDIS_URL: redis://redis:6379/0
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python -m celery -A app.tasks.celery_app worker --loglevel=warning --concurrency=1 -n worker@%h"

  beat:
    image: ontoprompt-agent-beat:agent-v1
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
      REDIS_URL: redis://redis:6379/0
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python -m celery -A app.tasks.celery_app beat --loglevel=warning"

  watchdog:
    image: ontoprompt-agent-watchdog:agent-v1
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
      REDIS_URL: redis://redis:6379/0
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python -m celery -A app.tasks.celery_app worker --loglevel=warning --concurrency=1 -n watchdog@%h"

  sweeper:
    image: ontoprompt-agent-sweeper:agent-v1
    depends_on:
      migration:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ontoprompt:ontoprompt@db:5432/ontoprompt
      REDIS_URL: redis://redis:6379/0
    command: /bin/sh -c "python scripts/verify_build_manifest.py --manifest /manifests/agent.json --expect-head 0015_external_mcp && exec python scripts/guarded_entrypoint.py -- python -m celery -A app.tasks.celery_app worker --loglevel=warning --concurrency=1 -n sweeper@%h"

  frontend:
    image: ontoprompt-agent-frontend:agent-v1
    ports: ["5173:4173"]
    depends_on:
      api:
        condition: service_started
    command: npm run preview -- --host 0.0.0.0
